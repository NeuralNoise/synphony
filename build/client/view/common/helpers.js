// Generated by CoffeeScript 1.3.1
(function() {

  define(['underscore', 'handlebars'], function(_, Handlebars) {
    var func, helpers, name;
    helpers = {
      link_to: function(context) {
        return "<a href='" + context.url + "'>" + context.body + "</a>";
      },
      each_pair: function(context, options) {
        return (_.map(context, function(value, key) {
          return options.fn({
            key: key,
            value: value
          });
        })).join("");
      }
    };
    for (name in helpers) {
      func = helpers[name];
      Handlebars.registerHelper(name, func);
    }
    return helpers;
  });

}).call(this);
