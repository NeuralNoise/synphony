// Generated by CoffeeScript 1.3.1
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

  define(['underscore', 'jquery', 'view/common/template', 'text!templates/common/sidebar.handlebars'], function(_, $, TemplateView, hbsTemplate) {
    var SidebarView;
    return SidebarView = (function(_super) {

      __extends(SidebarView, _super);

      SidebarView.name = 'SidebarView';

      function SidebarView() {
        return SidebarView.__super__.constructor.apply(this, arguments);
      }

      SidebarView.prototype.template = hbsTemplate;

      SidebarView.prototype.events = {
        "click .accordion": "accordion"
      };

      SidebarView.prototype.accordion = function(event) {
        var parent;
        event.preventDefault();
        parent = ($(event.target)).parent();
        parent.toggleClass('active');
        return parent.next().toggle('fast');
      };

      SidebarView.prototype.templateData = function() {
        return _.map(this.model.sections, function(value, key) {
          return {
            name: key,
            id: value.id
          };
        });
      };

      SidebarView.prototype.render = function() {
        var _this = this;
        SidebarView.__super__.render.apply(this, arguments);
        console.log("Rendering sidebar view ");
        _.each(this.model.sections, function(view) {
          return (_this.$('#' + view.id)).replaceWith(view.render().el);
        });
        return this;
      };

      return SidebarView;

    })(TemplateView);
  });

}).call(this);
