// Generated by CoffeeScript 1.3.1
(function() {

  define(['model/sequence', 'collection/sequence_elements'], function(Sequence, SequenceElements) {
    return describe("Sequence", function() {
      var sequence;
      sequence = null;
      beforeEach(function() {
        var elements;
        elements = new SequenceElements([
          {
            gpc: '1'
          }, {
            gpc: '2'
          }
        ]);
        return sequence = new Sequence({
          elements: elements
        });
      });
      return it("should exist", function() {
        var gpcs, sgpcs;
        gpcs = sequence.gpcs();
        sgpcs = sequence.elements().getGpcs();
        return (expect(gpcs)).toEqual(sgpcs);
      });
    });
  });

}).call(this);
