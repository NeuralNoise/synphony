// Generated by CoffeeScript 1.3.1
(function() {

  define(['model/gpc', 'collection/gpcs', 'collection/user_gpcs'], function(GPC, GPCs, UserGPCs) {
    return describe("UserGPCs", function() {
      var gpcs, ugpcs;
      ugpcs = null;
      gpcs = null;
      beforeEach(function() {
        var coll;
        gpcs = _.map(['a', 'b', 'c'], function(name) {
          return new GPC({
            name: name
          });
        });
        coll = new GPCs(gpcs);
        return ugpcs = new UserGPCs([], {
          gpcs: coll
        });
      });
      it("should return a list of known GPCs", function() {
        ugpcs.first().toggle();
        return (expect(ugpcs.getKnownGPCs())).toEqual([gpcs[0]]);
      });
      return it("should return a list of focus GPCs", function() {
        ugpcs.last().toggle();
        ugpcs.last().toggle();
        return (expect(ugpcs.getFocusGPCs())).toEqual([gpcs[2]]);
      });
    });
  });

}).call(this);
